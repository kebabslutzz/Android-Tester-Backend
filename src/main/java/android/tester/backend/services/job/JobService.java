package android.tester.backend.services.job;

import org.springframework.stereotype.Service;

@Service
public class JobService {

  public static void createJob() {
  }

//
//  for (ScreenShot screenShot : savedScreenShots) {
//    File imageFile = new File(imagePath + screenShot.getFileName());
//    File layoutFile = new File(layoutPath + screenShot.getFileName().replace(".png", ".json"));
//
//    // You need to construct these legacy objects or pass null/default if compatible
//    // Legacy mapping helpers might be needed for SystemContext/TestDevice list if deeply used by checkers
//
//    legacyAnalysisService.analyzeScreenshot(
//      screenShot,
//      imageFile,
//      layoutFile,
//      legacyApplication, // Mapped from your Application entity
//      dataFolder,
//      legacyTestDevices,
//      legacySystemContext
//    );
//  }

}
